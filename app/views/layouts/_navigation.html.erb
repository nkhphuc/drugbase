<nav class="navbar navbar-dark navbar-expand-md bg-dark">
    <div class="container-fluid">
        <%= link_to "Home", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <%= link_to "About", about_path, class: "nav-link active", "aria-current": "page" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Drugs database", drugs_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Workplace", workplaces_path, class: "nav-link" %>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Account
                    </a>
                    <ul class="dropdown-menu">
                        <% if current_user %>
                            <li> <%= link_to "Account Settings", edit_user_registration_path(current_user), class: "dropdown-item" %> </li>
                            <li> <%= button_to "Sign Out", signout_path, method: :delete, form: { data: { confirm: "Are you sure?" } }, class: "dropdown-item" %> </li>
                        <% else %>
                            <li> <%= link_to "Sign In", signin_path, class: "dropdown-item" %> </li>
                            <li> <%= link_to "Sign Up", signup_path, class: "dropdown-item" %> </li>
                        <% end %>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>